<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
    <link rel="stylesheet" href="/css/store/nav.css">
    <link rel="stylesheet" href="/css/store/product.css">
    <title>Cart</title>
</head>
<body>
    <%- include('./navbar'); %>
    <div class="container">
        <div class="row mt-2 pt-5 ms-5 me-5 row-bg">
            <% if(items.length!==0){ %>
                <% for(let item of items){ %>
                    <div class="card w-100 mb-1">
                        <div class="row">
                            <div class="col-md-4 d-flex justify-content-center">
                                <img src="<%= item.img %>" class="me-5 mt-2 mb-2 img-fluid" width="30%" alt="...">
                            </div>
                            <div class="col-md-8">
                                <div class="card-body d-flex justify-content-between align-items-center" style="height: 100%;">
                                    <h3 class="card-title"> <%= item.title %> </h3>
                                    <h5 class="card-text"> ₱<%= item.price.toLocaleString() %> x <%= item.quantity %> = ₱<%= (item.price*item.quantity).toLocaleString() %></h5>
                                </div>
                            </div>
                        </div>
                    </div>
                <% } %> 
            <% }else{ %>
                <h1>Cart is Empty</h1>
            <% } %>  
        </div>
    </div>   
</body>
</html>